# Plans — INDEX

Tactical *how* artifacts. The roadmap points here by path; detail never goes in the roadmap.

**Naming.** A point-in-time plan (a sprint plan, a trade study) is dated `YYYY-MM-DD-<slug>.md`. A
**living document** that is continuously updated is named by concept only — a date in the filename of a
document that never stops changing invites a dated near-duplicate later.

Delivered plans move to [../archives/plans/](../archives/plans/INDEX.md).

| File | Type | Summary |
| --- | --- | --- |
| [text-layer-first.md](./text-layer-first.md) | ACTIVE-SPEC | **Start here for M2.** The four-mode extraction contract — `text` / `ocr` / `both` / `auto` — with the CLI surface, the JSONL intermediate, the side-by-side comparison view, the five signals a reviewer uses to decide which output is better, and the three distinct "OCR returned nothing" cases. Grounded in the corpus census: 67% of pages need no recognition, and **24** pages must never be sent to a model at all (the older 482 was a conflation — § 2). **The text half is BUILT as of 2026-09-06**; § 11 records what was delivered and where it deliberately deviates from the spec. Ends with six open questions it deliberately does not answer. |
| [doctrine-compliance.md](./doctrine-compliance.md) | ACTIVE-SPEC | The bootstrap-doctrine gap list as a checklist with file paths. Skeleton is right; the memory folders were empty and the test floor protects the wrong code path. Four integrity items to do first — an untracked module, an untested CLI, two live classifiers for one concept, and a success fixture that is not in the repository. |
| [structural-faithfulness.md](./structural-faithfulness.md) | ACTIVE-SPEC | **Delivered 2026-09-05.** The second classifier axis: `ok/sparse/empty` answers *is there text?*, `intact/suspect` answers *is it faithful?*, and they never merge because their repairs differ — a suspect page must never be sent to OCR. Carries the output contract (a detector registry, evidence per page, an OR not a score), the swept constants, the two rejected signals with the measurements that rejected them — including the `-layout` delta, which ranks a prose-only syllabus *worse* than the broken fixture — and the measured false-positive rate in both directions. **Amended 2026-09-06:** four detectors now ship, and `shredded-lines` was re-swept — `_SHRED_MIN_RUN` 4 → 2 behind a rule that every run carry one genuine *mathematical* character, taking strict precision **85% → 93%** and recall **35% → 68%** together. Two of its own claims are now marked ⚠: the "control corpus" it swept `_SHRED_MAX_CHARS` against is full of real flattened mathematics, and the unmapped-glyph raw volume was off by 6×. |
| [latex-repair-and-validity.md](./latex-repair-and-validity.md) | ACTIVE-SPEC | **Delivered 2026-09-06.** Our own `latex_repair.py`, after an 11-bug audit ruled out vendoring MinerU's. Upstream *deletes* `\rightarrow` outright (and asymmetrically — `\Rightarrow` survives), annihilates compact `\left…\right` groups, and drops the evaluation bar from `\left. y \right\|_{x=0}`; `\Bar`→`\hat` is only #5. Over 44 recorded outputs it changes 4 and **all four are damage.** Also the validity axis that replaces compile-checking: eight detectors on the `Signal` roll-call, with `length-vs-crop` (tokens ÷ crop dimension) the measured winner at 8/10 and zero false positives — token-variety's headline 8/10 does not survive spacing-invariant tokenisation. **Wired 2026-09-06** as `ocr-handler check` (§ 7) — the fourth verb, free and GPU-less like `inspect`, reporting the roll-call and a per-rule repair diff and rewriting nothing unless asked. Running it over the recorded output found two things: `repair()` was spacing `\qquad` silently with no note (fixed), and a reading of nested empty `aligned` environments passed all seven detectors — **closed 2026-09-06** by an eighth, `content-free` (§ 3.8): content tokens are what survive stripping environment names, column specs, braces, alignment markers, spacing and delimiter sizing, and the floor of **3** is pinned by `x = 1`. It fires on 4 of 146 recorded model readings and **0** of 2,508 known-good corpus rows. A ratio arm and a length-gated floor were measured and rejected — both fire on a legitimate `x = 1` wrapped in `aligned`. The same sweep corrected § 7.4 (b): the reading's 14.4 tokens per line-width is *above* the best recorded good reading, 13.7, not below it. |
| [mask-first-crops.md](./mask-first-crops.md) | ACTIVE-SPEC | **Built 2026-09-05** as `src/ocr_handler/crops.py`. The largest measured accuracy win in the project that is *not* a model: feeding `ink.py`'s existing binary mask instead of raw pixels produced the best of 84 UniMERNet readings, recovering `\|\alpha\|^n` and the `\omega_0` subscripts and deleting a hallucinated `\frac{1}{2}` that was the slide's graph-paper grid read as a fraction bar. Also the padding result: a cliff at the *neighbour distance*, not a gradient. |
| [engine-backends.md](./engine-backends.md) | ACTIVE-SPEC | **Not built.** Answers *"there might be multiple OCR methods — a module for each?"*: yes, but **behind** `recognize.py`, not beside it — [ADR-0004](../decisions/0004-engine-choice-deferred-decide-by-head-to-head.md) mandates one swappable interface and the ~300-line cap forbids one file holding five engines. One backend per file under `src/ocr_handler/engines/`, each declaring as *data* what it needs — repo, weights bytes, **dtype with no default**, packages, free VRAM, and `degrades_below`/`degradation` — so a host is checked before anything loads. That last pair is the sm_75 finding made structural: PaddleX gates bf16 **and** SDPA at capability ≥ 8.0 and the target is Turing 7.5, so the Paddle path silently takes fp32 + eager; here it comes back as verdict `degraded` with the reason, never silence. Three preflight verdicts, not two, and **unknown is never "met"** — `nvidia-smi` is broken on the target, so the host probe prefers torch's CUDA-path `mem_get_info` and leaves fields `None` rather than assuming. Four errors replace one: `EngineUnavailable` / `EngineFailed` / `EmptyReading`, and the empty guard lives in the façade so `--mode ocr` can never return `""`. Routing is split three ways across three altitudes — *needs recognition?* (`textlayer`), *which of the three problems?* (the classifier), *which backend?* (one table lookup) — pinned by an AST test that only `cli` imports `recognize` and only `recognize` imports `engines`, which is what keeps the 430-document path GPU-free. **No fallback between engines, ever:** the prose path destroys 2-D maths. Ships now: the façade, the registry with an **empty** table and `DEFAULTS` all `None`, `base.py`, the `gpu_lock` move (its free-VRAM guard is a **no-op on the target today**). Waits: every `engines/<name>.py`, any default, `--engine`, and the dependency pin — *a `transformers >= 5` pin is a model choice wearing a different hat*. Adding the winner is one file + one table row + one default; no caller changes, and the "route not swap" outcome needs no flag either. **CLI cost: zero verbs, still four of five.** Two operator calls: the first subpackage under `src/ocr_handler/`, and whether a CPU **prose** backend is in scope — ADR-0004 does not block it. |
