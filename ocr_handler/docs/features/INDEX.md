# Features — INDEX

Specifications for **shipped-stable** capabilities: what it does, its interface, how to use it.

**EMPTY, deliberately.** Nothing is stable yet. `inspect` and `text` work but their surface is about to
change — `text` becomes `extract --mode text` in M2
([../plans/text-layer-first.md](../plans/text-layer-first.md) § 3). Writing a feature spec against an
interface that is scheduled to be renamed would create a doc that is wrong on arrival.

**Populate at M2**, with the `extract` contract, once the four modes exist and the CLI is settled.
Design lives in `docs/plans/` until then.
