# Session records — INDEX

Dated per-session narrative, `YYYY-MM-DD_<description>.md`. Immutable: written once, not revised.
Records move to [../archives/session_records/](../archives/session_records/INDEX.md) when stale.

**EMPTY** — and the reason is a convention that was never settled. This folder was created at project
init and never used; the one existing record was written straight into `archives/`:

| Date | Session | Where it is |
| --- | --- | --- |
| 2026-09-02 | Initialize, build the backbone, tear down Baidu Unlimited-OCR | [../archives/session_records/2026-09-02_initialize-and-teardown.md](../archives/session_records/2026-09-02_initialize-and-teardown.md) |

**The doctrine is inconsistent here and the operator should settle it.** `PROMPTS.md` § Initialize
specifies `docs/session_records/` archived *to* `docs/archives/session_records/`; the session-close prompt
writes directly into `archives/`. The exemplar project (`~/sys301_minesweeper/docs/`) uses this top-level
folder. Nothing was relocated unilaterally — see
[../plans/doctrine-compliance.md](../plans/doctrine-compliance.md) § C.
