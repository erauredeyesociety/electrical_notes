# Question 1

Find the mean, median, and mode for the sample whose observations, shown below, represent the number of sick days claimed on 9 income tax returns.

Data:

`9, 16, 13, 10, 4, 68, 20, 9, 14`

Which value appears to be the best measure of the center of these data? State reasons for your preference.

Captured platform state:

- The mean is x-bar = `18.11` — correct.
- The median is `13` — correct.
- Mode multiple-choice was attempted as “There is one mode. The mode is 68,” but that is not correct. Since `9` occurs twice and every other value occurs once, the correct mode is `9`.

Final dropdown sentence:

- Since the mean is ___ the median is ___ and the mode is ___ most of the data values, the ___ appears to be the best measure of the center.

Dropdown choices visible:

- For the median comparison: `greater than,` / `near,` / `less than,`
- For the mode comparison: `greater than` / `less than` / `near`
- Final measure: `mode` / `median` / `mean`

Rounding/entry notes:

- Mean: round to two decimal places as needed.
- Median: round to two decimal places as needed.
- Mode: do not round.
