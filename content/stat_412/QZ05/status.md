# QZ05 Status Notes

This file is the assignment-local map/status log.

## Screenshot wave

The initial timestamp screenshot wave was normalized in chronological order to:

- `1.png` through `9.png`

The capture order matches the quiz questions Q1--Q9.

OCR drafts and a contact sheet were generated in `tmp/`.

## Supporting reference files

- `1.md` through `6.md` are standard-normal reference tables linked from the
  first six quiz questions. The clean question prompts are `q1.md` through
  `q9.md`.

## Solved status

All nine supplied QZ05 prompts are captured and solved.

Platform review note: Q1--Q8 were accepted for full credit. Q9 was the only
non-accepted item, and its worked solution is intentionally retained as the
mathematical explanation for turn-in/review rather than being repeatedly
reworked to chase the platform entry format.

Normal probability answers use rounded z-table values. Q5 uses nearest-tenth
class boundaries because the prompt says sample means are recorded to the
nearest tenth. Q6(b) uses the usual central-90% normal cutoff
`z approximately 1.645`, giving `n = 121` after rounding up.
