\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}

\title{STAT 412 --- Quiz 4 Answer Key}
\author{}
\date{}

\begin{document}
\maketitle

\noindent\textbf{Source-status note.} All nine quiz prompts were captured from
the screenshots and pasted HTML. Every answer field was blank, so all answers
below are computed and independently verified, not platform-confirmed.

\begin{enumerate}[leftmargin=*,label=\textbf{\arabic*.}]
\item $\sigma_{-4X+6Y-8}^2=180$.

\item (a) $P(X\ge4/9)=\dfrac{875}{2187}$;\quad
(b) $h(y)=12y(1-y)^2$ for $0\le y\le1$;\quad
(c) $P(X<1/7\mid Y=3/4)=\dfrac{16}{49}$.

\item $\operatorname{Var}(X)=\dfrac1{72}$ and
$\sigma_X=\dfrac{\sqrt2}{12}$.

\item (a) \textbf{Choice A};\quad
(b) $E(Y)=9$, $E(Y^2)=\dfrac{247}{3}$,
$\operatorname{Var}(Y)=\dfrac43$.

\item (a) \textbf{Choice A}, enter
$f(x\mid y)=\dfrac{3x^2}{(3-y)^3}$;\quad
(b) $P(X>0.3\mid Y=1.7)=\dfrac{2170}{2197}\approx0.987711$.

\item $E(Z)=18.8$ and $\sigma_Z^2=80.36$.

\item (a) \textbf{Choice A},
$g(x)=\dfrac{10x+4}{9}$;\quad
(b) \textbf{Choice B},
$h(y)=\dfrac{8y+5}{9}$;\quad
(c) $P(X<1/3)=\dfrac{17}{81}$.

\item At most $12.5\%$; enter $12.5$.

\item (a) \textbf{Choice C},
$g(y,z)=\dfrac{6y^2z}{25}$;\quad
(b) \textbf{Choice C}, $h(y)=3y^2$;\quad
(c) $\dfrac{91}{900}$;\quad
(d) $\dfrac9{64}$.
\end{enumerate}

\end{document}
