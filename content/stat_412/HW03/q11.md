# Question 11

Impurities in batches of product of a chemical process reflect a serious
problem. It is known that the proportion of impurities \(Y\) in a batch has the
following density function. Complete parts (a) through (e).

\[
f(y)=
\begin{cases}
12(1-y)^{11}, & 0\le y\le1,\\
0, & \text{elsewhere}.
\end{cases}
\]

(a) Verify that the given function is a valid density function. Select the
correct choice and, if necessary, fill in its answer box.

- **A.** The function is valid because it is at least 0 everywhere **and** because
  \(\int_0^1 12(1-y)^{11}\,dy = [\text{answer}]\).
- B. The function is valid because it is at least 0 everywhere.
- C. The function is valid because \(\int_0^1 12(1-y)^{11}\,dy = [\text{answer}]\).

(b) A batch is considered not sellable and therefore not acceptable if the
percentage of impurities exceeds 55%. What is the probability that a batch is
considered not acceptable; that is, what is \(P(Y>0.55)\)?

The probability is [answer]. *(Round to four decimal places as needed.)*

(c) What are the parameters \(\alpha\) and \(\beta\) of the beta distribution
illustrated here?

\(\alpha = [\text{answer}]\), \(\beta = [\text{answer}]\). *(Type an integer or a
decimal. Do not round.)*

(d) The mean of the beta distribution is \(\dfrac{\alpha}{\alpha+\beta}\). What
is the mean proportion of impurities in the batch?

\(\mu = [\text{answer}]\). *(Round to four decimal places as needed.)*

(e) The variance of a beta-distributed random variable is
\(\sigma^2 = \dfrac{\alpha\beta}{(\alpha+\beta)^2(\alpha+\beta+1)}\). What is the
variance of \(Y\) in this problem?

\(\sigma^2 = [\text{answer}]\). *(Round to six decimal places as needed.)*

## Answers

- (a) Select **Choice A**; the integral equals \(1\) — graded correct on MyStatLab.
- (b) \(P(Y>0.55)=(1-0.55)^{12}=0.45^{12}=0.0001\) — graded correct on MyStatLab.
- (c) The density \(12(1-y)^{11}=\dfrac{1}{B(\alpha,\beta)}y^{\alpha-1}(1-y)^{\beta-1}\)
  gives **\(\alpha = 1\), \(\beta = 12\)** — graded correct on MyStatLab.
- (d) \(\mu = \dfrac{\alpha}{\alpha+\beta}=\dfrac{1}{13}=0.0769\) — computed here;
  the platform field was still empty.
- (e) \(\sigma^2 = \dfrac{\alpha\beta}{(\alpha+\beta)^2(\alpha+\beta+1)}
  = \dfrac{1\cdot 12}{13^2\cdot 14}=\dfrac{12}{2366}=0.005072\) — computed here;
  the platform field was still empty.

Sources:

- `11.png`
- `11-2.png`

All five parts (a)–(e) are now captured.
